// runtime code is wrapped by node by wrapper function
// not exactly like this approx like this.

(function (exports, require, module, __filename, __dirname){
    function wrapperFunction(){
        console.log('We are learning wrapper function')
    }
})
