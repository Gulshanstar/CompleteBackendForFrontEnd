const _ = require('underscore');

// pahle sochega ki yeah module hai
// 2nd file or folder hogga
// last me dekhega node_module pe.

var res = _.contains([1,40,56,32,12], 56);
console.log(res);

// remove the node_modules file and share with friends, your friend can run [npm i] command to install
// the all dependencies which is present in package .json file.